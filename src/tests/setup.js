// Vitest global setup file
// jest-dom matchers are intentionally NOT imported here to avoid
// compatibility issues with happy-dom. Tests that need DOM matchers
// can import @testing-library/jest-dom directly.
